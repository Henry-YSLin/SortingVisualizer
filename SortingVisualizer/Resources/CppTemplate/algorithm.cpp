#include "algorithm.hpp"

using namespace std;

char *getName()
{
    return "C++ Algorithm";
}


vector<Frame> runAlgorithm(int arr[], int size)
{
    vector<Frame> frames;
    
    // Write your algorithm here
    // When you want to visualize the array, use the following code:
    //    frames.push_back(newFrame(arr, size, {i}));
    // {i} is a list of indices of elements that are being accessed
    
    return frames;
}